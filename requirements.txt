# Core dependencies

# Deep Learning Framework
torch>=2.0.0

# Graph Neural Networks
torch-geometric>=2.4.0

# Scientific Computing
numpy>=1.24.0
tqdm>=4.65.0

# LLM Integration (for latent editor - cloud GPU only)
# Note: bitsandbytes requires CUDA (Linux GPU), skip on Mac
transformers>=4.36.0
peft>=0.7.0
accelerate>=0.25.0
# bitsandbytes>=0.41.0  # Uncomment on Linux with CUDA

# Optional: CAD File Processing
# cadquery>=2.3.0       # For L-bracket generation (already installed separately)

# Optional: Visualization
# matplotlib>=3.7.0

# Development
pytest>=7.0.0
