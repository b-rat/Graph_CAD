# Cloud GPU requirements for LLM Latent Editor training
# Tested on: AWS g5.4xlarge (A10G 24GB), A100 40GB, A40 48GB
# Install with: pip install -r requirements-cloud-gpu.txt

# Deep Learning Framework (CUDA version)
torch>=2.0.0
torchvision>=0.15.0

# Graph Neural Networks
torch-geometric>=2.4.0

# Scientific Computing
numpy>=1.24.0
scipy>=1.10.0  # Hungarian matching for transformer decoder
tqdm>=4.65.0

# CAD Geometry (required for data generation)
cadquery>=2.3.0

# LLM Integration (pinned versions for compatibility with 4-bit quantization)
transformers==4.40.0
peft==0.10.0
accelerate==0.28.0
bitsandbytes>=0.41.0  # 4-bit/8-bit quantization (requires CUDA)

# Training utilities
safetensors>=0.4.0
datasets>=2.14.0

# Testing
pytest>=7.0.0
